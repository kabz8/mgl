{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/milespace run build",
  "outputDirectory": "artifacts/milespace/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
